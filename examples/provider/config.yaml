apiVersion: v1
kind: Secret
metadata:
  name: example-provider-secret
type: Opaque
data:
  credentials: dGVzdAo=
---
apiVersion: template.crossplane.io/v1alpha1
kind: ProviderConfig
metadata:
  name: example
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: tarasque
      name: example-provider-secret
      key: credentials
---
